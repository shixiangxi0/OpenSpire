/**
 * evt — event-driven card game engine.
 *
 * Public API surface: createEngine() only.
 * Everything else is an internal implementation detail.
 */
export { createEngine } from './core/Engine.js'
